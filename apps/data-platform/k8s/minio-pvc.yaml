apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-data
  namespace: data-platform
spec:
  storageClassName: minio-local-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi