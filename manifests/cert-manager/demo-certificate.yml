apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: demo-local-tls
  namespace: demo
spec:
  secretName: demo-local-tls
  duration: 2160h
  renewBefore: 360h
  dnsNames:
    - demo.local
  issuerRef:
    name: selfsigned-cluster-issuer
    kind: ClusterIssuer